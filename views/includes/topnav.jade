//제일 윗 페이지 표시줄
.navbar.navbar-inverse.navbar-fixed-top
  .container
    .navbar-header
      button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#navbar', aria-expanded='false', aria-controls='navbar')
        span.sr-only Toggle navigation
        span.icon-bar
        span.icon-bar
        span.icon-bar
      a.navbar-brand(href='/') 
        i.fa.fa-home
    #navbar.collapse.navbar-collapse
      ul.nav.navbar-nav
        //만약 로그인이 안되어있다면 로그인페이지로
        //만약 로그인이 되어있다면 설문지만드는 페이지로
        //if (currentUser)
          //li 
          //목록을 누르면 todos로 가고 설문조사하는 페이로간다
          //a(href='/todos') 설문지 만들기 
          //a(href='/todos') 설문지 만들기
        //else
          //a(href='/signin') 설문지 만들기
        li
          a(href='/users') 사용자
      ul.nav.navbar-nav.navbar-right
        //현재 유저가 있으면
        if (currentUser) 
          li
            a(href='/users/#{currentUser._id}') Profile
          li
            a(href='/signout') Signout
        //else
          //li
            //a(href='/signin') 로그인
          //li
            //a(href='/users/new') 회원가입
          
